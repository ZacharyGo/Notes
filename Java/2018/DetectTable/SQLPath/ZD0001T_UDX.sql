/* ---------------------------------------------------------------------- */
-- ユニーク索引作成DDL(ORACLE用)
-- ファイル名：ZD0001T_UDX.sql
-- 対象テーブル（物理名）：ZD0001T_TBL
-- 対象テーブル（論理名）：帳票管理情報テーブル
/* ---------------------------------------------------------------------- */

CREATE UNIQUE INDEX ZD0001T_UDX01 ON ZD0001T_TBL(
	CHOHYO_KANRI_ID
)
/
