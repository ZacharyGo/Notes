/* ---------------------------------------------------------------------- */
-- ユニーク索引作成DDL(ORACLE用)
-- ファイル名：ZD0005M_UDX.sql
-- 対象テーブル（物理名）：ZD0005M_TBL
-- 対象テーブル（論理名）：電子帳票格納先マスタ
/* ---------------------------------------------------------------------- */

CREATE UNIQUE INDEX ZD0005M_UDX01 ON ZD0005M_TBL(
	CHOHYO_KAKUNO_SAKI_ID
)
/
